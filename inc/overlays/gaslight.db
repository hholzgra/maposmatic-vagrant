#! /bin/bash -e

pushd . > /dev/null

DBNAME="${DBNAMES[classic]:-gis}"

cd "$STYLEDIR"/Mapnik-fire-overlay

for f in sql-functions/*.sql
do
    sudo -u maposmatic psql "$DBNAME" --file="$f"
done

popd > /dev/null
